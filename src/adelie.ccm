export module adelie;

export import adelie.support.callable;
export import adelie.support.utils;
export import adelie.http;
export import adelie.routing.pattern;
export import adelie.routing.route;
export import adelie.routing.router;
export import adelie.auth;
export import adelie.facades.di;
export import adelie.facades.route;
export import adelie.facades.config;
export import adelie.db;
export import adelie.facades.db;
export import adelie.server;

export namespace adelie {

namespace di = facades::di;

using facades::Config;
using facades::DB;
using facades::Route;

using db::Model;
using db::ResultSet;
using db::Row;
using db::Session;
using db::Transaction;
using db::Value;

using http::Field;
using http::Header;
using http::Method;
using http::MethodSet;
using http::Request;
using http::Response;
using http::Status;

using auth::Authenticator;
using auth::MemorySessionStore;
using auth::SessionStore;
using auth::User;

using routing::Binding;
using routing::Constraint;
using routing::GroupAttributes;
using routing::Handler;
using routing::Middleware;
using routing::Next;
using routing::Parameter;
using routing::PendingRoute;
using routing::RouteDefinition;
using routing::Router;
using routing::RouteRegistrar;
using routing::UriPattern;

using server::Server;

}
